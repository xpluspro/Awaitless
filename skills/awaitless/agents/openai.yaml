interface:
  display_name: "Awaitless"
  short_description: "Adaptively run durable queued coding jobs"
  default_prompt: "Use $awaitless as the adaptive execution layer for this non-interactive work and consume any detached result durably."
