interface:
  display_name: "Awaitless"
  short_description: "Submit and consume durable queued coding jobs"
  default_prompt: "Use $awaitless to submit this long-running work once and consume its durable results."
