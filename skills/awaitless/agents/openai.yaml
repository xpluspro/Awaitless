interface:
  display_name: "Awaitless"
  short_description: "Submit and resume durable queued coding jobs"
  default_prompt: "Use $awaitless to submit this long command once, including a named queue when it must wait for a scarce resource."
